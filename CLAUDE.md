# 核心开发原则与指令 (System Instructions)



你在本仓库中编写、重构、排查或审查代码时，必须严格遵守以下最高铁律。拒绝盲目堆砌黑话，拒绝擅自大面积重构。



---



## 1. 唯一真理源 (Single Source of Truth, SSOT)

- **彻底贯彻 SSOT 原则**：任何一项数据、状态（如任务状态、系统配置、物理参数）在系统中有且仅有一个权威的、说了算的地方。

- **拒绝冗余与同步乱象**：绝不允许在不同模块、缓存（如 Redis）或内存变量中产生重复、冗余的状态拷贝。如果发现逻辑中存在数据不一致的隐患，优先重构消除冗余，以权威源（如数据库或统一参数 Provider）为准。



## 2. C++ 开发规范（现代 C++ 与 Google 风格）

- **代码风格**：严格遵循 **Google C++ Style Guide**（如变量小写加下划线 `snake_case`，类名 `PascalCase` 等）。

- **现代 C++ 风格**：强制使用 Modern C++ 特性（C++17/C++20/C++23，视项目支持而定）。严禁老旧的 C 风格写法。

- **内存与性能**：严格遵循“零开销原则”（Zero-overhead Principle）。注重所有权管理（智能指针）、优先使用右值引用与 move 语义，避免不必要的深拷贝（Zero-copy）。



## 3. 变量与符号命名规范（自解释性）

- **见名知意**：拒绝晦涩、随意、缺乏上下文的命名（严禁随手定义临时变量 `flag`, `tmp`, `val` 或单字母 `a`, `b`）。变量名必须直接反映其代表的物理意义、数学含义或业务逻辑。

- **可读性优先**：在遵守 Google 规范的前提下，优先保证可读性。如果简写会导致歧义，宁可把变量名写长。

- **明确坐标系与单位**：涉及自动驾驶/控制相关的变量，如果适用，必须在变量名中明确体现坐标系或单位（例如：`target_x_frenet`，`heading_error_rad`）。



## 4. 任务执行工作流（强制增加交互断点，防暴冲）

你**绝对禁止**一次性实现或修改大量功能。为了防止改动过大导致无法理解或回滚，必须遵循以下“步步为营”流程：

1. **先列计划**：在动手修改或编写任何代码前，必须先将整个任务拆解为连续的、原子化的**步骤文本列表**（Step-by-step list）。

2. **请求验证**：将步骤列表呈现给我，并使用特定句式明确询问：**“这是接下来的执行计划，是否确认？”**

3. **分步执行**：

   - 只有在我回复“确认”、“继续”或给出微调意见后，你才能开始执行**当前的第一步**。

   - 每完成一个原子步骤，必须停下来让我验证结果（如编译、运行测试）。在得到下一步授权前，**严禁**擅自推进后续代码。

5. 修改重构任何代码，不要删除我自己手动添加的注释

## 6. 严禁堆砌黑话，必须说人话

- **讲人话、禁谜语**：放弃高高在上的学术派或过度抽象的架构师黑话（如满嘴“终态、幂等、闭环、防御”却不接引具体上下文）。必须使用直白、具体的业务和工程语言进行解释。

- **伪代码先行**：如果某个技术方案比较抽象，必须立刻给出对应的 **核心伪代码（Pseudo-code）** 或 **一行真实代码** 来说明数据流转和谁调谁，禁止让我去猜你的大局观。

- **保持专业严谨**：在通俗易懂的同时，保持工程师的专业度。使用标准的行业大白话（如：孤儿进程、守护进程、秒级拉起），**禁止自己发明带有强烈情绪、戏谑或过于粗俗的非专业词汇**（如“野鬼进程”等）。 

